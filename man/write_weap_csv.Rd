% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_weap_csv.R
\name{write_weap_csv}
\alias{write_weap_csv}
\title{Escribir un data.frame en formato CSV de WEAP}
\usage{
write_weap_csv(df, file)
}
\arguments{
\item{df}{\code{data.frame} con columnas \code{Date}, \code{Year}, \code{Month}, \code{Day} y una por
subcuenca (por ejemplo la salida de \code{\link[=pcp2weap]{pcp2weap()}} o \code{\link[=tmp2weap]{tmp2weap()}}).}

\item{file}{Ruta del archivo CSV de salida.}
}
\value{
Devuelve \code{file} de forma invisible.
}
\description{
Escribe un \code{data.frame} en el formato de texto delimitado por comas que lee
el modelo WEAP mediante la funcion \code{ReadFromFile}. El archivo incluye las
cuatro directivas de cabecera (\verb{$ListSeparator}, \verb{$DecimalSymbol},
\verb{$DateFormat} y \verb{$Columns}) seguidas de los datos.
}
\details{
El \code{data.frame} de entrada debe tener como primeras columnas \code{Date}, \code{Year},
\code{Month} y \code{Day}, y a continuacion una columna por subcuenca. Habitualmente
se genera con \code{\link[=pcp2weap]{pcp2weap()}} o \code{\link[=tmp2weap]{tmp2weap()}}.
}
\examples{
\dontrun{
df <- pcp2weap("precip.nc", "subcuencas.shp", field = "Cod_WEAP")
write_weap_csv(df, "Datos_pcp.csv")
}
}
