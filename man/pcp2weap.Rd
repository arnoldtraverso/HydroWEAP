% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pcp2weap.R
\name{pcp2weap}
\alias{pcp2weap}
\title{Extraer precipitacion de un NetCDF al formato WEAP}
\usage{
pcp2weap(nc, subs, field, dates = NULL, digits = 2, file = NULL, fun = mean)
}
\arguments{
\item{nc}{Ruta(s) a archivo(s) NetCDF, o un objeto \code{SpatRaster}. Si se pasan
varias rutas se apilan en un unico \code{SpatRaster}.}

\item{subs}{Ruta a un shapefile de subcuencas, o un objeto \code{SpatVector} de
poligonos.}

\item{field}{Nombre del campo de \code{subs} que contiene el codigo WEAP de cada
subcuenca; se usa como nombre de columna en la salida.}

\item{dates}{Vector \code{Date} opcional con una fecha por capa temporal. Si es
\code{NULL} (por defecto) las fechas se leen de la dimension de tiempo del
NetCDF.}

\item{digits}{Numero de decimales al que redondear los valores.}

\item{file}{Ruta opcional del CSV de salida. Si se indica, el resultado se
escribe en formato WEAP con \code{\link[=write_weap_csv]{write_weap_csv()}} y el \code{data.frame} se
devuelve de forma invisible.}

\item{fun}{Funcion de agregacion aplicada a las celdas de cada subcuenca
(por defecto la media aritmetica).}
}
\value{
Un \code{data.frame} con columnas \code{Date}, \code{Year}, \code{Month}, \code{Day} y una
columna por subcuenca. Si se indica \code{file}, se devuelve de forma invisible.
}
\description{
Extrae series de precipitacion desde uno o varios archivos NetCDF grillados
(por ejemplo PISCO), calculando el promedio areal dentro de cada subcuenca,
y devuelve un \code{data.frame} listo para exportar al formato WEAP.
}
\examples{
\dontrun{
# Devolver el data.frame en memoria
pcp <- pcp2weap("precip.nc", "subcuencas.shp", field = "Cod_WEAP")

# Extraer y escribir el CSV de WEAP en un solo paso
pcp2weap("precip.nc", "subcuencas.shp", field = "Cod_WEAP",
         file = "Datos_pcp.csv")
}
}
