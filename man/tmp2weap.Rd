% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tmp2weap.R
\name{tmp2weap}
\alias{tmp2weap}
\title{Extraer temperatura media de NetCDF al formato WEAP}
\usage{
tmp2weap(nc_tmax, nc_tmin, subs, field, dates = NULL, digits = 2, file = NULL)
}
\arguments{
\item{nc_tmax}{Ruta(s) a NetCDF de temperatura maxima, o un \code{SpatRaster}.}

\item{nc_tmin}{Ruta(s) a NetCDF de temperatura minima, o un \code{SpatRaster}.
Debe cubrir exactamente el mismo periodo y numero de capas que \code{nc_tmax}.}

\item{subs}{Ruta a un shapefile de subcuencas, o un objeto \code{SpatVector} de
poligonos.}

\item{field}{Nombre del campo de \code{subs} que contiene el codigo WEAP de cada
subcuenca; se usa como nombre de columna en la salida.}

\item{dates}{Vector \code{Date} opcional con una fecha por capa temporal. Si es
\code{NULL} (por defecto) las fechas se leen de la dimension de tiempo de
\code{nc_tmax}.}

\item{digits}{Numero de decimales al que redondear los valores.}

\item{file}{Ruta opcional del CSV de salida. Si se indica, el resultado se
escribe en formato WEAP con \code{\link[=write_weap_csv]{write_weap_csv()}} y el \code{data.frame} se
devuelve de forma invisible.}
}
\value{
Un \code{data.frame} con columnas \code{Date}, \code{Year}, \code{Month}, \code{Day} y una
columna de temperatura media por subcuenca. Si se indica \code{file}, se
devuelve de forma invisible.
}
\description{
Extrae temperaturas maximas y minimas desde archivos NetCDF grillados (por
ejemplo PISCOt), calcula el promedio areal por subcuenca y devuelve la
temperatura media \code{Tmed = (Tmax + Tmin) / 2}, que es la variable que utiliza
WEAP. El resultado se entrega como un \code{data.frame} listo para exportar.
}
\examples{
\dontrun{
tmp2weap("tmax.nc", "tmin.nc", "subcuencas.shp", field = "Cod_WEAP",
         file = "Datos_tmed.csv")
}
}
